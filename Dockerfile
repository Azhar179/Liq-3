ERROR: failed to solve: process "/bin/sh -c yum update -y &&     yum install -y wget tar &&     echo \"Downloading Liquibase...\" &&     wget https://github.com/liquibase/liquibase/releases/download/v${LIQUIBASE_VERSION}/liquibase-${LIQUIBASE_VERSION}.tar.gz &&     echo \"Extracting Liquibase...\" &&     tar -xzf liquibase-${LIQUIBASE_VERSION}.tar.gz &&     mv liquibase-${LIQUIBASE_VERSION} liquibase &&     mv liquibase /usr/local/bin/ &&     rm liquibase-${LIQUIBASE_VERSION}.tar.gz" did not complete successfully: exit code: 2
