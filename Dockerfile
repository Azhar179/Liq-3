ERROR: failed to solve: process "/bin/sh -c echo \"Downloading Liquibase...\" &&     curl -L -O https://github.com/liquibase/liquibase/releases/download/v${LIQUIBASE_VERSION}/liquibase-${LIQUIBASE_VERSION}.tar.gz &&     tar -xzf liquibase-${LIQUIBASE_VERSION}.tar.gz &&     mv liquibase-${LIQUIBASE_VERSION} /usr/local/bin/liquibase &&     rm liquibase-${LIQUIBASE_VERSION}.tar.gz" did not complete successfully: exit code: 1
